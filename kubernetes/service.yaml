apiVersion: v1
kind: Service
metadata:
  name: cloud-native-service
spec:
  type: NodePort
  selector:
    app: cloud-native-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5050
      nodePort: 30007